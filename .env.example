# Copy to `.env` and fill in. These are referenced as ${VAR} from the YAML config.

# OAuth bearer token from your Robinhood Agent Account.
ROBINHOOD_MCP_TOKEN=

# Provide whichever LLM key matches your configured provider.
OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GROQ_API_KEY=
